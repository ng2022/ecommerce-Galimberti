

const CartWidget = () => {
    return (
        <img
        src={require('./icons/shopping-cart.png')}
        alt="chart icon"
        width="25px"
        />
    );
};

export default CartWidget;